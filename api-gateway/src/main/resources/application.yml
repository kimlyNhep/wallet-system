server:
  port: 8080

spring:
  main:
    web-application-type: reactive
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8081 # Replace with the actual URL of your Authentication Service
          predicates:
            - Path=/api/auth/v1/**
          filters:
            - RewritePath=/api/auth/v1/(?<segment>.*), /api/v1/$\{segment}
            - CustomHeaderFilter

        - id: wallet-service
          uri: http://localhost:8082 # Replace with the actual URL of your Wallet Service
          predicates:
            - Path=/api/wallet/v1/**
          filters:
            - RewritePath=/api/wallet/v1/(?<segment>.*), /api/v1/$\{segment}
            - CustomHeaderFilter
#
        - id: payment-service
          uri: http://localhost:8083 # Replace with the actual URL of your Transfer Service
          predicates:
            - Path=/api/payment/v1/**
          filters:
            - RewritePath=/api/payment/v1/(?<segment>.*), /api/v1/$\{segment}
            - CustomHeaderFilter
#
#        - id: transaction-history-service
#          uri: http://localhost:8084 # Replace with the actual URL of your Transaction History Service
#          predicates:
#            - Path=/api/v1/history/**
#          filters:
#            - RewritePath=/api/v1/history/(?<segment>.*), /$\{segment}
#            - CustomHeaderFilter
#
#        - id: notification-service
#          uri: http://localhost:8085 # Replace with the actual URL of your Notification Service
#          predicates:
#            - Path=/api/v1/notifications/**
#          filters:
#            - RewritePath=/api/v1/notifications/(?<segment>.*), /$\{segment}
#            - CustomHeaderFilter